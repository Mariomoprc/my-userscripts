# 计划：移除固定模型规则与固定模型

> 目标：撤销 `/模型` 命令、`model-whitelist.mjs`、`AGENTS.md` 中新增的"固定模型"逻辑，回归"模型在 web/TUI 界面手动切换"（与记忆库 LRN-20260810 记录一致）。其余改动（AGENTS.md 双语化、多模型适配、错误自学习闭环、实时搜索、单 provider 上限）保持不变。

---

## 一、现状确认

| 文件 | 固定模型相关内容 | 状态 |
|------|----------------|------|
| `opencode.jsonc` | 顶层 `model` 字段 | ✅ **无**（执行时未写入，无需改动） |
| `commands/模型.md` | §三-b 固定模型设置、§四 固定模型写入、§六 固定模型报告、description/注意事项 | ⚠️ 需移除 |
| `scripts/model-whitelist.mjs` | `pickRecommendedModel` 函数、`_recommended_model` 输出、固定模型推荐 stderr | ⚠️ 需移除 |
| `AGENTS.md` §8 | "`model` 字段为固定模型（Go 套餐最优）" | ⚠️ 需改写 |

**保留**（用户确认"其他部分保持不变"）：
- AGENTS.md 双语化、多模型适配、错误自学习闭环
- `/模型` 实时搜索（or-free-discover 必跑双源）
- 单 provider 上限 10（用户不确定，建议保留：独立于固定模型、防白名单膨胀、不影响手动切换；如需移除可后续再说）

---

## 二、改动清单

### 1. `commands/模型.md`（156行 → 约120行）

- **description**（第2行）：`智能筛选模型白名单 + 自动设置 Go 套餐固定模型（实时搜索评分，隐藏不常用模型）` → `智能筛选模型白名单（元数据实时评分），隐藏不常用模型`
- **第5行**：去掉"并在 Go 套餐已订阅时自动设置固定模型"
- **§三 stdout/stderr 描述**（71-72行）：去掉 `_recommended_model` 与"固定模型推荐"
- **删除 §三-b 整节**（76-100行）：固定模型设置（检测/四合一/写入 model 字段）
- **§四**（117-120行）：删除"固定模型（Go 已订阅时额外写入）"代码块；删除第125行"若已有顶层 model 且 Go 未订阅，不删除原有 model"
- **§五**（137行）：删除第3条"若设置了固定模型，验证 model 字段..."
- **§六**（145行）：删除"固定模型推荐"行
- **注意事项**（152行）：`本命令只改 provider.whitelist 和顶层 model（Go 已订阅时）` → `本命令只改 provider.whitelist`；删除第156行"固定模型探测依赖..."

### 2. `scripts/model-whitelist.mjs`（344行 → 约270行）

- **删除 `pickRecommendedModel` 函数**（220-249行）
- **主流程**（303-305行）：删除 `recommended`/`hasGoModels` 计算
- **删除固定模型推荐 stderr 块**（319-335行）
- **输出**（337-340行）：`const output = { ...result }; if (recommended) output._recommended_model = recommended; console.log(JSON.stringify(output, null, 2));` → `console.log(JSON.stringify(result, null, 2));`

### 3. `AGENTS.md` §8（151行）

- `**模型**：opencode.jsonc:provider 定义白名单（当前 4 个 provider），model 字段为固定模型（Go 套餐最优）。变更运行 /模型 自动评分+筛选。`
- → `**模型**：opencode.jsonc:provider 定义白名单（当前 4 个 provider），模型在 web/TUI 界面手动切换，不硬编码固定 model 字段（LRN-20260810）。变更运行 /模型 自动评分+筛选。`

### 4. `opencode.jsonc`

- 无需改动（确认无 `model` 字段即可）

---

## 三、验证

| 场景 | 操作 | 预期 |
|------|------|------|
| 脚本语法 | `node --check scripts/model-whitelist.mjs` | 无错误 |
| 脚本输出 | mock 输入运行 | stdout 仅 `{ providerId: [modelId...] }`，无 `_recommended_model` |
| 命令残留 | `grep -r "固定模型\|_recommended_model\|pickRecommendedModel" commands/模型.md scripts/model-whitelist.mjs AGENTS.md` | 无命中（记忆库归档除外） |
| AGENTS.md | 读 §8 | 无"固定模型"描述，改为手动切换 |
| opencode.jsonc | 读顶层 | 无 `model` 字段 |
| 实时搜索保留 | 读 §二 | or-free-discover 必跑逻辑仍在 |
| cap 10 保留 | 读 model-whitelist.mjs | 单 provider 上限逻辑仍在（若用户确认保留） |

---

## 四、关键文件

- 修改：`commands/模型.md`、`scripts/model-whitelist.mjs`、`AGENTS.md`
- 不动：`opencode.jsonc`、`memory-plugin.js`、`memory-plugin.json`、`commands/记住.md`、`commands/更新.md`、`skills/opencode-maintenance/SKILL.md`